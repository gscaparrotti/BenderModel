apply plugin: 'java'
apply plugin: 'maven'

repositories {
	mavenCentral()
}

sourceSets {
	main {
		java {
			srcDir 'src'
		}
	}
}

dependencies {
	compile "com.google.guava:guava:$guavaVersion"
}

defaultTasks 'wrapper', 'clean', 'check', 'build', 'assemble'
